Fix v-scores: scale sc fields from 0-10 to 0-100 for 535 entries

Root cause: mk() function in batches GZ-HAN used 0-10 scale for
emotional/relatability/etc scores, but the v4 formula in maintain.py
expects 0-100 scale (matching all pre-existing entries).

Fix: multiplied all sc field values by 10 for the 535 affected entries,
then re-ran maintain.py recalc_vs.

Before: v-scores ranged 4-10 (broken)
After:  v-scores ranged 43-92 (correct), mean 68.2, median 69

All 4497 ideas validated, JS-parseable, 0 dupes.
